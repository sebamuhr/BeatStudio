# Beat Studio (native desktop) — CORE Python deps (enough to run the app).
# The installer also adds the system audio library (libportaudio2 / portaudio).
PySide6-Essentials>=6.7
numpy>=1.26
scipy>=1.11
mido>=1.3
python-rtmidi>=1.5     # MIDI backend for mido → hardware control surfaces (e.g. Akai APC Key 25)
sounddevice>=0.4
soundfile>=0.12

# ---- Optional: AI sound matching (CLAP). Heavy, NOT needed to run. ----
# Enable with:  ./install.sh --with-ai
# (equivalent manual steps:)
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install transformers
